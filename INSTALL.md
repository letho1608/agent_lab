# Installation Guide

## Requirements

- Python 3.9+
- Microsoft Visual Studio Build Tools (for compiled extensions)
- Dependencies listed in `requirements.txt`

## Installation Steps

1. Clone the repository
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   pip install .
   ```
